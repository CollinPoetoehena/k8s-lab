---
# defaults file for roles/mgmt_vm

# Azure CLI
install_azure_cli: true

# Ansible installation
ansible_version: "2.15.0"
install_ansible: true

# SSH configuration
mgmt_vm_ssh_keys_path: "/home/ansible/.ssh"
cluster_ssh_private_key: "{{ mgmt_vm_ssh_keys_path }}/cluster_key"
